<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="200dp" android:layout_height="200dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/product_name_label"
        android:text="@string/dialog_name"
        android:textColor="@android:color/black"
        android:layout_marginTop="30dp"
        android:layout_alignParentLeft="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/product_count_label"
        android:text="@string/dialog_count"
        android:textColor="@android:color/black"
        android:layout_marginTop="30dp"
        android:layout_below="@id/product_name_label"
        android:layout_alignParentLeft="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/product_price_label"
        android:text="@string/dialog_price"
        android:layout_marginTop="30dp"
        android:layout_below="@id/product_count_label"
        android:textColor="@android:color/black"
        android:layout_alignParentLeft="true" />

    <EditText
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/product_count"
        android:inputType="number"
        android:layout_alignBaseline="@+id/product_count_label"
        android:layout_alignBottom="@+id/product_count_label"
        android:layout_marginLeft="50dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/black" />

    <EditText
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/product_price"
        android:inputType="numberDecimal"
        android:layout_alignBaseline="@+id/product_price_label"
        android:layout_alignBottom="@+id/product_price_label"
        android:layout_marginStart="50dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/black" />

    <EditText
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/product_name"
        android:inputType="text"
        android:background="@android:color/darker_gray"
        android:layout_marginStart="50dp"
        android:layout_alignBaseline="@+id/product_name_label"
        android:layout_alignBottom="@+id/product_name_label"
        android:layout_toEndOf="@+id/add_button" />


</RelativeLayout>